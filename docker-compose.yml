services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      TZ: "UTC"
    volumes:
      - ./ahorcado:/var/www/ahorcado
      - ./php.ini:/usr/local/etc/php/conf.d/zz-custom.ini:ro
